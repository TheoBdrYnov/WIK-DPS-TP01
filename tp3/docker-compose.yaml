services:
  api:
    build: .
    expose:
      - "8000"
    deploy:
      replicas: 4

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
